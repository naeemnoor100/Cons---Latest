# Server Configuration
PORT=3000
NODE_ENV=production
SESSION_SECRET=your-secret-key-here

# Database Configuration
# Set DB_TYPE to 'mysql' to use MySQL instead of SQLite
DB_TYPE=sqlite

# MySQL Configuration (if DB_TYPE=mysql)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=buildtrack_pro
